import { SimpleContentPage } from "@/components/woven-client";

export const metadata = {
  title: "Returns | Woven",
  description: "Woven returns and exchange information.",
};

export default function Page() {
  return <SimpleContentPage type="legal" />;
}
