import { SimpleContentPage } from "@/components/woven-client";

export const metadata = {
  title: "About | Woven",
  description: "The Woven story and student-first brand manifesto.",
};

export default function Page() {
  return <SimpleContentPage type="about" />;
}
